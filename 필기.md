# Next.js의 4가지 캐시 메커니즘

Next.js는 매우 공격적으로 캐싱을 수행합니다. 크게 네 가지 레벨에서 데이터와 페이지를 캐싱하며, 각각 목적과 동작 방식이 다릅니다.

---

## 1. 요청 기억(Request Memorization)

- **개념**: 동일한 설정으로 발생하는 데이터 요청을 서버가 기억하여, 같은 요청이 중복해서 실행되지 않도록 하는 메커니즘
- **범위**: **단일 요청 처리 동안만** 유지됨 (하나의 HTTP 요청 사이클 내)
- **목적**: 데이터 소스에 불필요한 중복 요청 방지

---

## 2. 데이터 캐시(Data Cache)

- **개념**: 데이터 소스의 값이 변하지 않았을 경우, 이전에 불러온 데이터를 저장하고 재사용
- **범위**: 사용자가 재검증하거나 설정된 시간이 만료될 때까지 지속
- **목적**: 데이터 소스로의 왕복 요청 자체를 피함 → 성능 최적화
- **특징**: 요청 기억은 “중복 요청 방지”, 데이터 캐시는 “요청 자체 회피”

---

## 3. 전체 라우트 캐시(Full Route Cache)

- **개념**: 단순히 데이터만 저장하는 것이 아니라, **전체 페이지(HTML, React Server Component Payload)**를 캐싱
- **효과**:
  - 데이터 재요청 불필요
  - 전체 HTML 재렌더링도 피할 수 있음
- **범위**: 관련 데이터 캐시가 재검증되기 전까지 유지
- **장점**: 업데이트된 데이터가 있을 때만 새롭게 페이지를 렌더링

---

## 4. 라우터 캐시(Router Cache)

- **개념**: 브라우저 메모리에 React Server Component Payload 일부를 저장하는 **클라이언트 측 캐시**
- **차이점**: 앞의 세 가지 캐시는 모두 서버 관리, 라우터 캐시는 브라우저가 관리
- **목적**: 페이지 간 이동 속도 향상 (서버에 매번 요청할 필요 없음)
- **무효화 조건**:
  - 서버에서 새로운 페이지가 렌더링될 때
  - 사용자가 Next.js 사이트를 벗어났다 돌아올 때

---

## 핵심 요약

- **요청 기억**: 단일 요청 내에서 중복 요청 방지
- **데이터 캐시**: 데이터 변경 전까지 요청 자체 회피
- **전체 라우트 캐시**: HTML/서버 컴포넌트까지 포함해 전체 페이지 재사용
- **라우터 캐시**: 클라이언트 메모리에 저장하여 페이지 이동 속도 최적화

---

@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@

# Next.js 14 → 15: unstable_noStore vs connection 정리

## unstable_noStore (Next.js 14)

- 위치: `next/cache`
- 역할: 정적 프리렌더/캐시에서 제외 (동적 페이지 강제)
- 특징:
  - 캐시 금지 + 정적 프리렌더 차단을 동시에 의미
  - 명령형 API, unstable이라 권장되지 않음
- 사용 예시:

```tsx
import { unstable_noStore } from "next/cache";

export default async function Page() {
  unstable_noStore(); // 정적 프리렌더 + 캐시 금지
  const res = await fetch("https://api.example.com", {
    cache: "no-store",
  });
  const data = await res.json();
  return <pre>{JSON.stringify(data)}</pre>;
}
```

---

## connection (Next.js 15부터 권장)

- 위치: `next/server`
- 역할: 정적 프리렌더를 금지하고, 매 요청 시 동적 렌더링 수행
- 특징:
  - 렌더링 전략만 제어 (데이터 캐시 정책은 fetch 옵션으로 따로 지정)
  - 공식적으로 권장되는 stable API
- 사용 예시:

```tsx
import { connection } from "next/server";

export default async function Page() {
  await connection(); // 정적 프리렌더 차단, 런타임 요청마다 렌더
  const res = await fetch("https://api.example.com", {
    cache: "no-store", // 항상 최신 데이터
    // next: { revalidate: 60 }, // 60초 주기 캐시 재검증
  });
  const data = await res.json();
  return <pre>{JSON.stringify(data)}</pre>;
}
```

---

## 차이점 요약

| 구분        | unstable_noStore (14)                 | connection (15)                               |
| ----------- | ------------------------------------- | --------------------------------------------- |
| 위치        | next/cache                            | next/server                                   |
| 성격        | 명령형, unstable                      | 선언적, stable                                |
| 역할        | 정적 프리렌더 차단 + 사실상 캐시 금지 | 정적 프리렌더 차단만                          |
| 데이터 정책 | 암묵적으로 no-store 성격 강함         | fetch 옵션(`no-store`, `revalidate`)으로 제어 |
| 권장 여부   | ❌ 비권장 (레거시)                    | ✅ 공식 권장                                  |

# fetch cache 옵션 정리

## fetch cache 옵션

### 1. cache: "default"
- Next.js 기본 동작
- 빌드 시 정적으로 프리렌더링하고, 데이터는 라우트/데이터 캐시에 저장됨
- 정적 페이지에 적합

### 2. cache: "force-cache"
- 데이터를 강제로 캐싱
- fetch가 항상 캐시된 데이터를 사용 (가능하다면 재요청 안 함)
- 주로 변경이 거의 없는 정적 리소스에 적합

### 3. cache: "no-store"
- 캐시를 완전히 무시하고 항상 새로 요청
- 실시간 데이터, 사용자 맞춤 데이터에 적합
- 정적 프리렌더 차단 효과도 있음

### 4. next: { revalidate: N }
- 지정한 초 단위마다 캐시를 재검증
- ISR(Incremental Static Regeneration) 방식
- 예: `revalidate: 60` → 60초마다 데이터 새로고침

---

## Next.js의 4가지 캐시 정리 (Request Memoization, Data Cache, Full Route Cache, Route Cache)

### 한눈에 표

| 캐시위치/관리 주체 | 범위 | 수명 | 목적 | 무효화(예) | 주의점/팁 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Request Memoization** | 서버(요청 컨텍스트) | 단일 요청 동안만 | 같은 요청 내 중복 fetch/연산 제거 | 자동(요청 종료 시 소멸) | “데이터 보관” 아님. 성능 최적화용 dedupe 수준 |
| **Data Cache** | 서버(영속 캐시) | 재검증 전까지(분~시간) | 데이터 재사용으로 백엔드 왕복 감소 | next.revalidate, revalidatePath, revalidateTag, 수동 무효화 | cache: "no-store"면 대상 아님. 태그 설계하면 다경로 무효화 쉬움 |
| **Full Route Cache** | 서버(HTML/RSC 결과) | 관련 Data Cache가 유효할 때 | 페이지 전체 결과(HTML + RSC payload) 재사용 | 관련 데이터 캐시가 재검증/무효화될 때 | 정적 프리렌더. connection()/dynamic="force-dynamic"로 제외 가능 |
| **Route Cache (Router Cache)** | 클라이언트(브라우저 메모리) | 탭 세션/이동 간 | 라우트 전환 속도 향상(RSC payload 캐싱) | 새 서버 렌더 발생, 사이트 이탈/복귀 등 | 서버 캐시와 별개. 화면 자동 갱신 원하면 router.refresh()나 실시간 채널 필요 |

---

### 1) Request Memoization
- **개념**: 하나의 서버 요청 처리 중 동일 키의 fetch/비싼 함수 호출을 한 번만 실행하고 결과를 공유.
- **범위**: 단일 요청(한 HTTP 요청 사이클) 내부에서만 유효. 요청이 끝나면 사라짐.
- **목적**: 같은 데이터를 같은 요청 중에 여러 컴포넌트가 요구해도 중복 호출 방지.
- **오해 방지**: 영속 캐시가 아니라 일회성 dedupe.

### 2) Data Cache
- **개념**: fetch 결과나 계산 결과를 요청 간에도 재사용하는 영속 데이터 캐시.
- **채우는 법**: 기본 force-cache/default, next: { revalidate: N }, "use cache"/unstable_cache 등.
- **무효화/갱신**:
    - **시간 기반**: next.revalidate: N(ISR) → N초 후 다음 요청 시 백그라운드 갱신.
    - **이벤트 기반**: revalidatePath("/path"), revalidateTag("tag") (쓰기 직후 즉시 최신화).
- **예외**: cache: "no-store"는 Data Cache를 쓰지 않음(매 요청 네트워크).

### 3) Full Route Cache (정적 프리렌더 결과 캐시)
- **개념**: 데이터만이 아니라 **페이지 전체 산출물(HTML + React Server Components payload)**을 캐싱.
- **효과**: 서버는 렌더를 생략하고 완성본을 바로 서빙 → 매우 빠름.
- **수명**: 이 페이지가 의존하는 Data Cache가 유효한 동안.
- **무효화**: 의존 데이터가 재검증/무효화되면 페이지 결과도 폐기되어 다시 렌더.
- **동적 전환**: 정적 제외가 필요하면 connection()(Next 15 권장) 또는 export const dynamic = "force-dynamic"(Next 14~)로 Full Route Cache에서 제외.

### 4) Route Cache (클라이언트 라우터 캐시)
- **개념**: 브라우저(앱 라우터)가 최근 방문한 라우트의 RSC payload 일부를 메모리에 저장.
- **목적**: 페이지 간 전환을 체감상 즉시로 만들기.
- **무효화**: 새 서버 렌더가 발생하거나, 앱을 떠났다가 돌아오면 비워질 수 있음.
- **중요**: 서버 캐시와 별개. 열린 탭 자동 갱신은 안 됨 → 필요 시 router.refresh(), 폴링, WebSocket/SSE 병행.

---

### 언제 무엇을 쓰나 (현업 기준)
- **중복 호출만 줄이고 싶다** → Request Memoization (자동).
- **대부분 캐시로 빨리 주고, 가끔만 새로 받으면 된다** → Data Cache + next.revalidate(ISR).
- **쓰기 발생 시 즉시 최신 페이지가 필요** → Data Cache에 태그/경로 무효화: revalidateTag/revalidatePath.
- **페이지를 정적으로 서빙하고 싶다** → Full Route Cache 기본값 유지.
- **사용자/요청별로 항상 새로 렌더해야 한다** → connection()(또는 dynamic="force-dynamic").
- **탭 내 전환을 빠르게** → Route Cache는 자동 동작. 다만 최신 보장을 원하면 router.refresh()나 실시간 채널을 추가.

---

### 흔한 오해 & 팁
- **“revalidate: N이면 N초마다 자동 갱신?”** → 아니고, 만료 후 첫 요청 시 백그라운드 갱신.
- **revalidatePath 하면 열려 있는 탭이 즉시 바뀜?** → 아님. 서버 캐시만 무효화. 클라에서 redirect/router.refresh()가 필요.
- **no-store vs connection()**:
    - `no-store` = 데이터 캐시 끔(항상 네트워크).
    - `connection()` = 정적 프리렌더 제외(렌더 전략). 둘은 역할이 다름. 같이 쓸 수도 있음.
- **여러 경로가 같은 데이터 의존** → `fetch(..., { next: { tags: ["messages"] } })`와 `revalidateTag("messages")`로 일괄 무효화가 더 낫다.
- **실시간 UI** → 캐시만으론 안 됨. 폴링/SSE/WebSocket 등 이벤트 채널 필요.

---

### 최소 레시피(예)
- **캐시형 목록 + 작성 시 즉시 갱신**
    - 목록 fetch: `next: { revalidate: 60, tags: ["messages"] }`
    - 작성 서버 액션: `revalidateTag("messages")` 후 redirect 또는 클라 router.refresh()
- **권한/개인화 페이지(항상 동적)**
    - 상단에 `connection()`(15) 또는 `dynamic="force-dynamic"`(14)
    - 데이터 신선도는 별도로 `no-store` 또는 `revalidate`

이렇게 쓰면 4개 캐시의 역할이 안 겹치고, 어디서 무엇을 무효화해야 하는지도 명확해진다.

---

## unstable_cache vs “use cache”

### unstable_cache (Next.js 14까지)
- **위치**: `next/cache`
- **용도**: 특정 함수의 실행 결과를 캐싱
- **작성 방식**:
  ```javascript
  import { unstable_cache } from "next/cache";
  import { cache } from "react";

  export const getMessages = unstable_cache(
    cache(async function getMessages() {
      return db.prepare("SELECT * FROM messages").all();
    }),
    ["messages"],                // 고유 keyParts
    { tags: ["msg"], revalidate: 60 } // 옵션
  );
  ```
- **특징**:
  - `keyParts` 배열을 수동으로 지정해야 캐시 키가 달라짐
  - `tags`, `revalidate` 옵션을 함께 제공
  - 함수 인자가 자동으로 키에 반영되지 않음 (별도로 `keyParts`에 넣어야 함)
  - 이름 그대로 unstable (비권장)

---

### “use cache” (Next.js 15부터 공식 권장)
- **위치**: 함수 파일 최상단 `"use cache"` 디렉티브
- **용도**: 함수 호출 결과를 자동으로 캐싱
- **작성 방식**:
  ```javascript
  "use cache";
  import { unstable_cacheLife as cacheLife, unstable_cacheTag as cacheTag } from "next/cache";

  export async function getMessages() {
    cacheLife({ revalidate: 60 });  // 데이터 재검증 주기
    cacheTag("msg");                // 캐시 태그
    return db.prepare("SELECT * FROM messages").all();
  }
  ```
- **특징**:
  - `"use cache"`를 선언하면 함수 전체가 캐싱 대상
  - 함수 인자 값이 자동으로 캐시 키에 포함됨 → `keyParts` 필요 없음
  - 캐시 설정은 함수 내부에서 `cacheLife()` / `cacheTag()`로 지정
  - stable API (앞으로 공식 권장)

---

### 차이점 요약

| 구분 | unstable_cache (14) | “use cache” (15) |
| :--- | :--- | :--- |
| **상태** | unstable (비권장) | stable (권장) |
| **선언 위치** | 함수 정의 외부에서 감쌈 | 함수 내부 "use cache" 디렉티브 |
| **캐시 키** | `keyParts` 수동 지정 | 함수 인자/클로저 값 자동 반영 |
| **재검증 설정** | `options.revalidate` | `cacheLife({ revalidate })` |
| **태그 설정** | `options.tags` | `cacheTag("...")` |
| **무효화 지원** | `revalidateTag()` 가능 | `revalidateTag()` 가능 |
| **사용 난이도** | boilerplate 많음 | 간결, 자동화 |

---

### 활용 팁
- **데이터 변경 이벤트 기반** → `revalidateTag("msg")`로 실시간 무효화
- **주기적 자동 재검증** → `cacheLife({ revalidate: 60 })`
- **특정 페이지 무효화** → `revalidatePath("/messages")`
